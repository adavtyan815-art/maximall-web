/**
 * MongoDB has been removed. This file is kept as an empty stub so that
 * any legacy imports do not cause module-not-found errors during transition.
 * It is safe to delete this file and the /data/models/ directory.
 */
export async function connectDB(): Promise<void> {
  // No-op — in-memory store requires no DB connection
}
